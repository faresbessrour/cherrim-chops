class Light:
    def __init__(state):
        self.state = state

    def change_state():
        self.state = (self.state + 1)  % 4
        return true

    def color():
        return "YELLOW" if self.state == 2 else "GREEN" if self.sate == 1 else "RED"
